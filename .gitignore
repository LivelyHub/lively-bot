# Secrets — never commit
.env
.env.*
!.env.example
*.key
*.pem

# Node
node_modules/
dist/
*.tsbuildinfo

# Baileys WhatsApp auth state (session credentials — never commit)
auth/

# Persisted conversation memory
data/

# OS / editor
.DS_Store
*.log
.idea/
.vscode/

# Local planning artifacts (specs/plans) — kept private during the competition
docs/superpowers/
.superpowers/

# Local session handoff (not published)
HANDOFF.md
